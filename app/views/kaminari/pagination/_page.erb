<% if page.current? %>
  <div class="btn btn-sm" disabled="disabled">
    <%= page %>
  </div>
<% else %>
  <%= link_to page, url, remote: remote, rel: page.rel, class: "btn btn-ghost btn-sm" %>
<% end %>